<h1>All The Guests</h1>

<h2> <%= link_to "Create a new appearance", new_guest_path %></h2>

<ul>
  <% @guests.each do |guest| %>
    <li><%= link_to guest.name, guest %></li>
  <% end %>
</ul>
<!-- did not create the rating yet.  will create in the table and adust the DB then input it here  -->
